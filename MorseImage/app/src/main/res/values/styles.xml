<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>

        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
        <item name="android:windowContentOverlay">@drawable/solid_line</item>
    </style>

    <style name="AppTheme.ActionBar" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:displayOptions">showCustom</item>
    </style>



    <style name="horizontal_gray_divider">
        <item name="android:layout_height">1px</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:background">@color/grey_line</item>
    </style>


    <style name="sign_input_style">
        <item name="android:layout_height">60dp</item>
        <item name="android:textSize">19sp</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@null</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="android:padding">5dip</item>
    </style>

    <style name="progress_bar_style">
        <item name="android:minWidth">100.0dip</item>
        <item name="android:minHeight">100.0dip</item>
        <item name="android:indeterminateDrawable">@drawable/dialog_loading_animation</item>
        <item name="android:maxWidth">100.0dip</item>
        <item name="android:maxHeight">100.0dip</item>
    </style>

    <!-- 加载对话框样式 -->
    <style name="load_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 对话框背景 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

</resources>
