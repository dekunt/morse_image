<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.meu.morseimage">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".phpTest.activity.NoteListActivity"
            android:label="@string/note_list_title"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".phpTest.activity.LoginActivity"
            android:label="@string/login_title"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".phpTest.activity.ChangePswActivity"
            android:label="@string/change_psw_title"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".phpTest.activity.RegistActivity"
            android:label="@string/regist_title"
            android:screenOrientation="portrait"/>

        <activity android:name=".phpTest.activity.NoteEditActivity"
                  android:configChanges="orientation|keyboard|screenSize"
                  android:screenOrientation="portrait"
                  android:windowSoftInputMode="stateVisible|adjustResize"/>

    </application>

</manifest>
